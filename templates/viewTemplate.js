const {
  buildIndexViewTemplate,
  buildLoginViewTemplate,
  buildRegisterViewTemplate,
} = require("../generators/templates");

module.exports = {
  buildIndexViewTemplate,
  buildLoginViewTemplate,
  buildRegisterViewTemplate,
};
